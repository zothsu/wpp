---
import { Image } from "@/components/starwind/image";

interface Program {
  image: string;
  title: string;
  tagline: string;
  href: string;
}

const programs: Program[] = [
  {
    image: "/images/todler.jpg",
    title: '"Sprouts" Infant/Toddler',
    tagline: "Ages 0-2",
    href: "/programs/infant-toddler",
  },
  {
    image: "/images/kristin-brown-3kBtYrZ0-hw-unsplash.jpg",
    title: "Preschool",
    tagline: "Ages 3-5",
    href: "/programs/preschool",
  },
  {
    image: "/images/alexandr-podvalny-bh476ojZw2g-unsplash.jpg",
    title: "School Age Summer Camp",
    tagline: "Ages 5-10",
    href: "/programs/summer-camp",
  },
];
---

<section class="@container mx-auto max-w-6xl px-4 py-24">
  <div>
    <h2>Our Programs</h2>
    <p class="text-muted-foreground mt-4 text-pretty @xl:text-lg">
      From infancy through school age, every program at Wild Pear is rooted in
      outdoor exploration and hands-on discovery.
    </p>
  </div>

  <ul class="mt-12 grid gap-6 @xl:grid-cols-3">
    {
      programs.map((program) => (
        <li>
          <a
            href={program.href}
            class="group relative block aspect-3/4 overflow-hidden rounded-2xl"
          >
            <Image
              src={program.image}
              alt={program.title}
              width={800}
              height={1067}
              class="size-full object-cover"
            />
            <div class="absolute inset-0 bg-linear-to-t from-black/80 via-black/20 to-transparent" />
            <div class="absolute inset-x-0 bottom-0 p-6">
              <p class="text-sm font-medium text-white/70">{program.tagline}</p>
              <h3 class="mt-2 text-xl font-semibold text-white">
                {program.title}
              </h3>
            </div>
          </a>
        </li>
      ))
    }
  </ul>
</section>
