---
import "../styles/starwind.css";
import Navbar4, { type NavLink } from "../components/starwind-pro/navbar-04/Navbar4.astro";
import SectionSwitcher from "../components/SectionSwitcher.astro";
import SiteFooter from "../components/SiteFooter.astro";
import ShaderNightClouds from "../components/starwind-pro/shader-night-clouds/ShaderNightClouds.astro";

interface Props {
	title: string;
}

const { title } = Astro.props;

const links: NavLink[] = [
	{
		label: "Forms",
		children: [
			{ label: "Enrollment", href: "/forms/enrollment" },
			{ label: "Other Forms", href: "/forms/other" },
		],
	},
	{ label: "Tuition", href: "/tuition" },
	{ label: "Resources", href: "/resources" },
];
---

<html lang="en" class="dark">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<link rel="icon" href="/favicon.ico" />
		<meta name="viewport" content="width=device-width" />
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>
	</head>
	<body>
		<ShaderNightClouds />
		<Navbar4 siteName="Wild Pear" links={links} ctaText="Contact" ctaHref="/contact">
			<SectionSwitcher current="enrolled" id="section-switcher-desktop" slot="section-switcher" />
			<SectionSwitcher
				current="enrolled"
				id="section-switcher-mobile"
				slot="section-switcher-mobile"
			/>
		</Navbar4>
		<slot />
		<SiteFooter />
	</body>
</html>
